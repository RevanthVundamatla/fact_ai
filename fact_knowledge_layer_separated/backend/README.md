# Backend

FastAPI service for PDF ingestion, fact extraction, normalization, relationship reasoning, persistence, and REST APIs.

Entry point: `main.py`

Run from the project root:

```bash
python backend/run.py
```
